# Static site — no build step. Netlify serves the repo root as-is.
[build]
  publish = "."
  command = ""

# Pretty URLs: /projects resolves to /projects.html
[[redirects]]
  from = "/projects"
  to = "/projects.html"
  status = 200
